import { Metrophobic } from 'next/font/google';
import { NextFont } from 'next/dist/compiled/@next/font';

export const metrophobic: NextFont = Metrophobic({
    weight: '400',
    subsets: ['latin']
  });